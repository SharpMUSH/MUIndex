# What compose reads beside compose.yaml. .env.example is the committed half; this one holds the
# database password of whoever cloned this.
.env

# The Cloudflare Origin CA certificate and its key, pasted onto the server. The key is a key.
deploy/tls/

bin/
obj/
.vs/
.idea/
*.user
appsettings.*.local.json

# Backfill artifacts. The importer is code and is committed; what it harvests is not (spec §7.6).
imports/
*.import.json
*.import.ndjson
